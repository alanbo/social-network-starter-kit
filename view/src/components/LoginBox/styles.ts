import { makeStyles } from '@material-ui/styles';

export default makeStyles({
  input: {
    marginTop: 20,
    marginBottom: 20,
    width: '100%'
  },

  login_form_wrapper: {
    maxWidth: '500px',
    width: '300px',
    display: 'flex',
    flexWrap: 'wrap',
    justifyContent: 'flex-end'
  },

  button: {}
});

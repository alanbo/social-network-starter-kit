type User {
  first_name: String!,
  last_name: String!,
  email: String!,
  gender: String!,
  ip_address: String!,
  createdAt: String!,
}

type Query {
  user(email: String!): User 
}

type Mutation {
  login(email: String!, password: String!): User!
  logout: Boolean!
}
